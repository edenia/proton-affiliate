apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: protonaffiliate-hapi
  name: protonaffiliate-hapi
spec:
  replicas: 1
  selector:
    matchLabels:
      app: protonaffiliate-hapi
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: protonaffiliate-hapi
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - image: ${DOCKER_REGISTRY}/${IMAGE_NAME_HAPI}:${VERSION}
          imagePullPolicy: 'Always'
          name: 'protonaffiliate-hapi'
          envFrom:
            - configMapRef:
                name: protonaffiliate-hapi-config
          ports:
            - containerPort: 9090
