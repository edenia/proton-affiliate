apiVersion: v1
kind: Service
metadata:
  labels:
    app: protonaffiliate-webapp
  name: protonaffiliate-webapp
spec:
  ports:
    - port: 80
      targetPort: 80
      name: http
      protocol: TCP
  selector:
    app: protonaffiliate-webapp
