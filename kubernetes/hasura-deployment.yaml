apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: protonaffiliate-hasura
  name: protonaffiliate-hasura
spec:
  replicas: 1
  selector:
    matchLabels:
      app: protonaffiliate-hasura
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: protonaffiliate-hasura
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - args:
            - graphql-engine
            - serve
            - --enable-console
          envFrom:
            - configMapRef:
                name: protonaffiliate-hasura-config
          image: ${DOCKER_REGISTRY}/${IMAGE_NAME_HASURA}:${VERSION}
          imagePullPolicy: 'IfNotPresent'
          name: 'protonaffiliate-hasura'
          ports:
            - containerPort: 8080
      restartPolicy: Always
